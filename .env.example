# Ganti dengan private key walletmu (WAJIB)
PRIVATE_KEY=0x...

# URL RPC untuk jaringan 0G Testnet (bisa diganti jika ada yang lebih cepat)
RPC_URL=https://evmrpc-testnet.0g.ai

# URL Indexer untuk 0G Storage
INDEXER_URL=https://indexer-storage-testnet-turbo.0g.ai

# URL Explorer untuk melihat transaksi
EXPLORER_URL=https://chainscan-galileo.0g.ai/tx/

# Berapa kali script akan menjalankan proses upload
UPLOADS_TO_RUN=5

# Biaya pendaftaran file on-chain dalam satuan ETHER (bukan wei).
# Ini adalah pengganti nilai gas/value yang di-hardcode.
# Mungkin perlu disesuaikan jika ada perubahan dari 0G.
STORAGE_FEE_IN_ETHER=0.00084

# Jeda waktu antar upload dalam milidetik (misal: 5000 = 5 detik)
DELAY_MS=5000
